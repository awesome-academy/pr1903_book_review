<p><%= review.rating %></p>
<p><%= review.comment %></p>

<%= link_to "Edit" , edit_book_review_path(review.book_id, review) %>
<%= link_to "Delete" , book_review_path(review.book_id, review ) , method: :delete , :onclick=> "return confirm('Are you sure you want to delete this item?')"  %>
